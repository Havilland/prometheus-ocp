---
apiVersion: {{ cluster_prometheus_apiGroup }}/v1
kind: ServiceMonitor
metadata:
  name: pushgateway-servicemonitor
  labels:
    prometheus: monitoring
spec:
  selector:
    matchLabels:
      name: pushgateway
  endpoints:
  - port: http
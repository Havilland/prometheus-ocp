---
- debug:
    msg: "Variable: {{ item.metadata.name }}"

- name: deploy service account
  k8s:
    verify_ssl: "{{ lookup('env','K8S_AUTH_VERIFY_SSL') }}"
    api_version:
    
#- name: create clusterrolebinding
#  k8s:
#    verify_ssl: "{{ lookup('env','K8S_AUTH_VERIFY_SSL') }}"
#    api_version:
#
#- name: deploy prometheus cr to namespace
#  k8s:
#    verify_ssl: "{{ lookup('env','K8S_AUTH_VERIFY_SSL') }}"
#    api_version:
#    
#- name: deploy pushgateway
#  k8s:
#    verify_ssl: "{{ lookup('env','K8S_AUTH_VERIFY_SSL') }}"
#    api_version:
#    
#- name: deploy pushgateway servicemonitor
#  k8s:
#    verify_ssl: "{{ lookup('env','K8S_AUTH_VERIFY_SSL') }}"
#    api_version:
#    
#- name: deploy grafana
#  k8s:
#    verify_ssl: "{{ lookup('env','K8S_AUTH_VERIFY_SSL') }}"
#    api_version:
    
